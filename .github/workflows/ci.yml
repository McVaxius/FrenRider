name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
          dotnet-quality: 'preview'

      - name: Install Dalamud runtime
        uses: Blooym/setup-dalamud@v1
        with:
          branch: release

      - name: Restore
        run: dotnet restore .\FrenRider.sln

      - name: Build (Release)
        run: dotnet build .\FrenRider.sln -c Release --no-restore

      - name: Upload packaged plugin zip
        uses: actions/upload-artifact@v4
        with:
          name: FrenRider-latest
          path: .\FrenRider\bin\x64\Release\FrenRider\latest.zip
          if-no-files-found: error
